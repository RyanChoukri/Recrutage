<%= form_for(instance) do |f| %>
  <% if instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instance.errors.count, "error") %> prohibited this instance from being saved:</h2>

      <ul>
      <% instance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!--   <div class="form-group">
    <%= f.label :id_instance %>
    <%= f.number_field :id_instance %>
  </div> -->


          <div class="form-group">
            <label class="col-lg-2 control-label">Questionaire</label>
            <div class="col-lg-10">
              <select class="form-control" name="instance[id_survey]">
                <% @surveys.each do |survey| %>
                <option value="<%= survey.id %>"><%= survey.name %></option>
                <% end %>
              </select>
            </div>
          </div>

  <div class="form-group">
    <%= f.label :year %>
    <%= f.number_field :year %>
  </div>

  <div class="form-group">
    <%= f.label :month %>
    <%= f.number_field :month %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>