<div class="panel panel-default">
	<div class="panel-heading">Le questionnaire</div>
	<div class="panel-body">
		
	<% if @answer != nil %>

		<% @my.each do |key, value| %>
   			<p>Qestion : <%= key %> - Reponse : <%= value %></p>
		<% end %>


	<% end %>


		<form method="POST" action="/sendquestion">
			<%= hidden_field_tag :authenticity_token, form_authenticity_token %>

			<div class="form-group">
				<div class="col-lg-10">
					<%= @survey.html.html_safe  %>
				</div>
			</div>


			<div class="form-group">
				<div class="col-lg-10">
					<input class="btn btn-block btn-success" name="commit" value="Envoyé votre questionnaire" type="submit">
				</div>
			</div>

		</form>

	</div>
</div>